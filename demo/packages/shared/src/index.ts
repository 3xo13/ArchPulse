/**
 * shared/src/index.ts
 *
 * Public API of the shared layer.
 * No dependencies on other demo packages.
 */
export { makeUserId, makeMoney } from "./types.js";
export type { UserId, ProductId, Money } from "./types.js";
